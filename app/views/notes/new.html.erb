<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-5 pb-2 mb-3 border-bottom">
    <h1 class="h2">New Note for <%= @contact.name%></h1>
</div>

<div class="w-50">
    <%= form_for [@contact, @note] do |f|%>
        <div class="form-group">
            <%= f.label        :name %>
            <%= f.text_field   :name, :class=>"form-control", :placeholder => "John Doe"%> 
        </div>
        <div class="form-group">
            <%= f.label        :body %>
            <%= f.text_area   :body, :class=>"form-control", :placeholder=>"john@example.com" %>
        </div>       

        <%= f.submit "Create Note", :class=>"btn btn-primary" %>
    <% end %>
</div>